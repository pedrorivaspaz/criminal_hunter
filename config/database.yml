default: &default
  adapter: postgresql
  encoding: unicode
  host: <%= ENV['host'] %>
  port: <%= ENV['port'] %>
  database: <%= ENV['database'] %>
  username: <%= ENV['username'] %>
  password: <%= ENV['password'] %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
